# BlogSpace
This full-stack blogging web app utilizes Node.js, Express, and MongoDB under an MVC architecture. It features secure Passport.js authentication and custom middleware for role-based authorization (User, Owner, Admin). It supports full post/review CRUD, Multer/Cloudinary media uploads, and Joi server-side schema validation.
